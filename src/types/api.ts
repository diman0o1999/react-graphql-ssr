export interface SignInClientType {
  signInClient: string | null;
}

export interface SignInClientVariables {
  email: string;
  // password: string;
}

export interface ItemObject {
  email: string;
  // password: string;
}
